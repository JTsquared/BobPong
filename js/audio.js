// ============================================================
// BobPong - Audio Manager (placeholder for future sound effects)
// ============================================================

export class AudioManager {
    constructor() {
        this.enabled = false;
        this.sounds = {};
    }

    // Future: load and play sounds
    // play(name) { ... }
    // loadSound(name, src) { ... }
}
